Failed to create stream fd: Operation not permitted
Failed to create stream fd: Operation not permitted
Failed to create stream fd: Operation not permitted
# C 题项目执行规则

## 事实源与分支

- 上游为 `stars-spark/C--PRCTICE`。
- Claude 原工作分支为 `claude/national-math-modeling-c-g6d060`。
- Codex 接管工作使用独立分支，不直接覆盖 Claude 分支或 `main`。
- 修改前先检查活动进程和 `git status`。正在执行的脚本不得原地编辑。

## 建模硬约束

1. 每个决策时刻只能使用当时可获得的信息。不得使用当日或未来真实负载、光伏、价格校准当时的计划。
2. 储能状态必须从 2025-01-01 00:00 的 6000 kWh 连续演化到年底。输出虽从 2 月 1 日开始，1 月也必须用于热启动和状态传递。
3. 功率统一为 kW，10 分钟能量统一乘以 `DT=1/6 h` 后以 kWh 输出。附件列和模板列按原序号映射，不手写时段偏移。
4. 计划、执行和事后评价三层必须分开。实际执行允许的储能再调度规则必须写成明确的优化或确定性规则。
5. 问题三每次调整的目标函数必须包含调整费用和预期紧急购电成本。增加可选信息或可选决策机会后，最优成本不得因强制使用信息而恶化；“不调整”必须始终是可行候选。
6. 参数校准使用滚动历史或明确的训练集，不得用评价期未来误差。边界最优必须扩大搜索区间后再下结论。
7. 若不使用二进制变量约束充放电互斥，必须检查每个时段的同时充放电量，并给出避免退化解的证明或微小吞吐惩罚。

## 结果与论文同步

- 五份结果工作簿必须由代码从题目模板生成，不手填主数据区。
- 每次提交前检查非空率、维度、日期范围、连续紧急购电区间合并、汇总列和论文数字一致性。
- 论文要求的指定日期结果必须按题面表 1、表 2、表 3 的字段完整呈现，不能只给日合计。
- 摘要中方法名称必须与实际模型一致。确定性滚动 LP 不称为随机规划。
- 论文声称“完整代码见 code/”时，仓库和最终提交必须确实包含该目录。

